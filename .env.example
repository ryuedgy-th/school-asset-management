# Environment variables template
# Copy this file to .env and fill in your actual values
# NEVER commit .env to Git!

# Database
# For local development with PostgreSQL:
DATABASE_URL="postgresql://postgres:password@localhost:5432/school_asset_dev"

# For Neon (production/staging):
# DATABASE_URL="postgresql://user:pass@ep-xxx.neon.tech/dbname?sslmode=require"

# Authentication (CRITICAL: Generate strong random secrets!)
# Generate with: openssl rand -base64 32
AUTH_SECRET=your-super-secret-auth-key-here-change-me

# Encryption Secret (CRITICAL: Generate strong random secret!)
# Generate with: openssl rand -base64 32
ENCRYPTION_SECRET=your-encryption-secret-here-change-me

# ============================================================
# GOOGLE OAUTH (Optional - For SSO Login)
# ============================================================
# Get credentials from: https://console.cloud.google.com
# Used for NextAuth Google Sign-In
# AUTH_GOOGLE_ID=your-google-client-id.apps.googleusercontent.com
# AUTH_GOOGLE_SECRET=your-google-client-secret

# ============================================================
# APP URL (Optional)
# ============================================================
# NEXT_PUBLIC_APP_URL=https://your-domain.com

# ============================================================
# EMAIL CONFIGURATION - Managed via Database UI
# ============================================================
# ⚠️  DO NOT configure email here! Use the Database UI instead:
# Settings → Email & Integration → Email Accounts
#
# The system supports:
# - SMTP (with encrypted password storage)
# - Google OAuth (Gmail API)
#
# Benefits of Database UI:
# - Encrypted credentials storage
# - Multiple email accounts support
# - Easy switching between accounts
# - No server restart required
#
# Legacy fallback (only if no email account in database):
# EMAIL_SERVER_HOST=smtp.gmail.com
# EMAIL_SERVER_PORT=587
# EMAIL_SERVER_USER=your-email@gmail.com
# EMAIL_SERVER_PASSWORD=your-app-password-here
# EMAIL_FROM=your-email@gmail.com

# ============================================================
# DEPRECATED - Now managed via Database UI
# ============================================================
# The following settings are now managed through the web UI:
# - Scan Passcode: Settings → Security
# - Email Recipients: Settings → Email & Integration → Recipients
# - Email Accounts: Settings → Email & Integration → Email Accounts
#
# Legacy variables (kept for reference only):
# SCAN_PASSCODE=your-scan-passcode-here
# DIRECTOR1_EMAIL=director1@school.com
# DIRECTOR2_EMAIL=director2@school.com
# IT_HEAD_EMAIL=it.head@school.com
